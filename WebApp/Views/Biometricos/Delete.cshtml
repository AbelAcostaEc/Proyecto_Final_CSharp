@using Modelo.Entidades

@model Biometrico
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Eliminar de Biometrico</h1>

<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h3>
                Eliminar Biometrico
            </h3>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-10">
            <form asp-action="Delete" method="post">
                <input type="text" asp-for="BiometricoId" hidden />

                <div class="form-group">
                    <label class="custom-control-label">Hora Entrada: </label>
                    <input class="form-control" type="datetime-local" asp-for="Entrada" disabled />
                </div>
                <div class="form-group">
                    <label class="custom-control-label">Hora Salida: </label>
                    <input class="form-control" type="datetime-local" asp-for="Salida" disabled />
                </div>
                <div class="form-group">
                    <label class="custom-control-label">Fecha: </label>
                    <input class="form-control" type="date" asp-for="Fecha" disabled />
                </div>
                <div class="form-group">
                    <label class="custom-control-label">Empleado: </label>
                    <select class="form-control"
                            asp-for="EmpleadoId" asp-items="@ViewBag.selectListEmpleados" disabled>
                        <option value="">--Seleccione un empleado---</option>
                    </select>
                </div>

                <div class="form-group">
                    <input class="btn btn-primary" type="submit" value="Eliminar Biometrico" />
                </div>
            </form>
        </div>
    </div>

</div>

